//importing the dom 
import ReactDOM from 'react-dom/client';
//adding the css
import './index.css';
//impprting the App component
import App from './App';

//creating the root element
const root = ReactDOM.createRoot(document.getElementById('root'));
//rendering the app, although the code can also be written in a functional way
root.render(<App />); develop
